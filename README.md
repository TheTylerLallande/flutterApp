# Insert Button App

A minimal Flutter app containing a single enabled button. Its callback is intentionally empty.

## Run it

Install the [Flutter SDK](https://docs.flutter.dev/get-started/install), then from this folder run:

```sh
flutter create .
flutter run
```

`flutter create .` adds platform-specific runner files without changing the application code in `lib/main.dart`.
